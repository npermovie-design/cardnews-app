-- ============================================================================
-- N-CRIT-3 완전 수정: 구버전 change_points_atomic(text) 오버로드 제거
-- 구버전은 auth.uid() 검증 없이 아무 사용자 포인트 변경 + 양수 허용
-- PostgREST가 text 인자를 우선 매칭할 수 있어 보안 우회 경로
-- ============================================================================

DROP FUNCTION IF EXISTS change_points_atomic(text, integer, text);
